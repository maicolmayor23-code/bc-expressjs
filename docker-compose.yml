services:
  mongodb:
    image: mongo:7.0
    container_name: dj_sound_lights_mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:

